version: "3.9"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./indexes:/app/indexes
      - ./data:/app/data
    environment:
      - LOG_LEVEL=info
    restart: unless-stopped